<%# without the equals sign after the percent sign indicates it will not be output to the client side, only run in the server %>
<% if (locals.errorMessage != null) { %>
    <%= errorMessage %>
<% } %>